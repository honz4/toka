#! /usr/bin/toka

#! ================================================
#! A simple, almost V7 compatible version of the 
#! 'echo' utility. 
#!
#! Todo:
#!   Add -n flag (no trailing newline)
#! ================================================

[ #args [ i arglist get-element type space ] +iterate cr bye ] invoke
