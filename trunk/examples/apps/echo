#! /usr/bin/toka

#! ================================================
#! A simple, almost V7 compatible version of the 
#! 'echo' utility. 
#!
#! Todo:
#!   Add -n flag (no trailing newline)
#! ================================================

[ #args [ i arglist array.get type space ] +iterate cr bye ] invoke
