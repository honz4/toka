t: Garbage collection
t:   (gc :gc malloc keep)
   cr
t:   You should see  1 temporary object, and
t:   no trash in the list below:
   cr gc gc gc gc gc 
   15 malloc drop
   :gc cr
t:   You should see no temporary or trash objects
   cr gc :gc
